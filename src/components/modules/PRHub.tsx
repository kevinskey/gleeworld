import { PRCoordinatorHub } from '@/components/pr-coordinator/PRCoordinatorHub';

// PR Hub — Control Center entry point for public relations tooling.
// Renders the full PR coordinator suite (gallery, analytics, social media,
// press releases, press kits, tags) — same engine as the member-view module.
export const PRHub = () => <PRCoordinatorHub />;

export default PRHub;
