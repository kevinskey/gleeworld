# Open [VERIFY] questions for owner review

Generated 2026-07-08 from the authoring panel. Resolve these before final ship.

67 items:

- (factsheet) [factsheet:platform] Whether self-serve plan checkout is live yet — code has all PLAN_CHECKOUT_LINKS null (GleeWorldLanding.tsx), so tiers show 'Coming soon — talk to us'; confirm no separate checkout path exists elsewhere.
- (factsheet) [factsheet:platform] Exact behavior/content of TenantLanding vs GleeWorldLanding for non-main tenants (TenantLanding.tsx not read in detail).
- (factsheet) [factsheet:platform] What HouseHome (the /dashboard landing content) actually shows to each role — only its route wiring was confirmed, not its rendered widgets.
- (factsheet) [factsheet:platform] Android app parity — an android/ directory exists but was not inspected; iOS was the focus.
- (factsheet) [factsheet:platform] The '9 core features' referenced in pricing copy are not enumerated in code that was read; the list is marketing copy.
- (factsheet) [factsheet:platform] Whether the 20% educational discount is applied automatically at checkout or handled manually (only stated as marketing copy).
- (factsheet) [factsheet:platform] RecordingLiveActivityPlugin is disabled in MainViewController; confirm Live Activity recording is not user-facing in current build.
- (factsheet) [factsheet:platform] Full role list and how roles are assigned/stored (roles are read from JWT/profile; the assignment flow was not traced).
- (factsheet) [factsheet:roles-accounts] pages/Auth.tsx (which renders <AuthTabs>) is imported in App.tsx:139 but no <Auth /> route usage was found via grep; AuthTabs is only routed at /auth/mus240 via Mus240Auth.tsx. Confirm whether pages/Auth.tsx is dead code or reached through some path other than /auth (which uses AuthPage).
- (factsheet) [factsheet:roles-accounts] pages/ResetPassword.tsx:150 hardcodes the heading 'Riverside Concert Choir' instead of using tenant branding — confirm whether this is a live tenant-neutrality bug or intentional for a specific tenant.
- (factsheet) [factsheet:roles-accounts] The exact list of roles a super-admin/director can assign, and the UI/flow for provisioning student/admin accounts (the 'superadmin provisioning API' referenced by must_change_password), live server-side and were not found in this src/ tree — verify the admin user-management screens (e.g. useAdminUserRecords, useUsers) if the manual needs to cover admin-side account creation.
- (factsheet) [factsheet:roles-accounts] Password-strength rules differ across surfaces: ResetPassword requires ≥6 chars; ForcePasswordChange requires ≥8 + upper/lower/number; ChangePasswordDialog also requires a special character. Confirm which is the intended canonical policy for the manual.
- (factsheet) [factsheet:roles-accounts] The Jan 2026 forced-password-change window (localStorage 'password_changed_jan2026', deadline copy) may be a legacy one-time campaign rather than an ongoing behavior — confirm whether /force-password-change is still reached only via the must_change_password metadata flag today.
- (factsheet) [factsheet:roles-accounts] 'member' vs 'student' and 'graduate' vs 'alumni'/'graduates' appear as coexisting role strings in pickDestination and useUserRole; confirm the canonical stored value(s) in gw_profiles.role so docs don't describe roles that no longer get written.
- (factsheet) [factsheet:tenants] Is the custom-domain editing UI reachable anywhere live? The only custom-domain editor found is in the dead/unrouted SiteSetup.tsx (gw_tenants.custom_domain, CNAME→gleeworld.org flow); confirm whether any mounted page lets a tenant set a custom domain.
- (factsheet) [factsheet:tenants] Tenant data export (General tab): confirm whether the gw-tenant-export edge function now exists — WorkspaceSettingsPage.tsx:933-939 currently only shows a toast and comments that it is not yet wired.
- (factsheet) [factsheet:tenants] /admin/events (EventManagement) is a static placeholder; confirm the real event-creation path for directors is CreateEventDialog inside /dashboard/calendar and that /admin/events should not be documented.
- (factsheet) [factsheet:tenants] Where is CreateEventDialog actually launched from in the calendar UI, and does event creation offer an option to enable QR/attendance for that event, or is attendance always available for any gw_events row?
- (factsheet) [factsheet:tenants] StudentOnboarding reads settings.organization_name (StudentOnboarding.tsx:26) but the branding schema elsewhere uses org_name — confirm which column actually holds the org name so the invite email shows the right program name.
- (factsheet) [factsheet:tenants] Branding is editable in two places with different logo affordances: WorkspaceSettings Branding tab (logo URL text field) vs the page builder (theme colors/fonts, no logo). Confirm the intended/primary place a director uploads a logo file, since the file-upload SiteSetup page is dead code.
- (factsheet) [factsheet:tenants] Exact role/permission required to reach /admin/students, /admin/students/onboard, /admin/public-page, and /dashboard/workspace — routes are wrapped only in ProtectedRoute (any signed-in user); confirm whether in-page gating or nav hiding restricts these to admins.
- (factsheet) [factsheet:tenants] Plan enforcement: is the student_cap actually enforced on invite/creation (blocking new students at cap), or is the 'At cap' message purely advisory in Workspace Settings?
- (factsheet) [factsheet:tenants] Confirm the live media-library experience for directors: /admin/media (MediaLibrary) vs /dashboard/media-library (MediaLibraryPage) — which is current and what folder-sharing/upload features each exposes.
- (factsheet) [factsheet:students] Plan/entitlement gating: the /dashboard/* and /studio and /practice-studio routes are guarded only by ProtectedRoute (auth). Whether a student on a starter-tier tenant can actually reach Part Tracks Studio / Studio / Practice Studio depends on module visibility (v_tenant_active_modules) and possibly nav filtering not fully traced. Confirm whether these tools are hidden or hard-blocked for tenants without the relevant add-on, or merely absent from nav.
- (factsheet) [factsheet:students] Studio session persistence/back end: useStudio.ts (useMySessions/useCreateStudioSession/useStudioOwner) was not fully read — the underlying table/RPC (e.g. gw_studio_sessions) and whether sessions are private-per-user or shareable/collaborative is unconfirmed. StudioHome copy says sessions 'sync across your devices' but sharing was not verified.
- (factsheet) [factsheet:students] COURSE_ROUTES in AttendanceScanPage/QRScanner is a hardcoded course-id → route map (MUS 070/240/210/etc.) — confirm whether this is Spelman-tenant-specific hardcoding or generalized for all tenants before documenting course redirect behavior generically.
- (factsheet) [factsheet:students] The exact RPC response fields (success/message/event_info/course_id/checkout/status) for process_qr_attendance_scan, process_qr_checkout_scan, and process_pin_attendance_scan were inferred from the client; the server-side rules (geofence enforcement, duplicate-scan handling, token expiry, check-in vs checkout window) live in DB functions not read here.
- (factsheet) [factsheet:students] The distinction and current recommended path between the two Part Tracks surfaces (the /dashboard/part-tracks Studio vs. the 'Part Tracks' module tile using RecordModal/PartMixer) — confirm which is the intended student-facing one for the manual, since both are wired up.
- (factsheet) [factsheet:students] StudentAssignmentView.updateMutation navigates back to a hardcoded '/academy/mus-240' after submission (StudentAssignmentView.tsx:151-153) regardless of course — verify whether this is a known limitation or tenant-specific before documenting the post-submit flow.
- (factsheet) [factsheet:students] AssignmentViewer / RecordingStudio (Practice Studio 'practice' and 'record' views) and the sight-reading AssignmentSubmissionModal were not read in detail — their exact record/submit steps should be confirmed if the manual covers them.
- (factsheet) [factsheet:students] MusicLibraryPage is described as read-only for students because edit/upload is gated by canEditMusicLibrary(); confirm the exact role logic in useUserRole so the manual states correctly which roles (member/student vs librarian/admin) can upload.
- (factsheet) [factsheet:fans] Application fee: box-office-checkout sets a 1% Stripe application_fee_amount (index.ts:203-211), but the user's saved memory says 'Box Office is intentionally 0% fee.' Confirm which is currently true in production before documenting the fee to fans.
- (factsheet) [factsheet:fans] Ticket confirmation email: both fan pages promise tickets 'by email,' but no supabase/function in this repo sends the box-office ticket email (send-order-email is the merch store; stripe-webhook does not handle box office). Fulfillment is an external port-3030 Connect webhook calling gw_box_office_fulfill_order (migration 20260621160000). Confirm what confirmation email the buyer actually receives and from where.
- (factsheet) [factsheet:fans] How a fan discovers the /program/:slug URL in practice (QR at the concert vs shared link) — code confirms QR generation in the admin editor but not real-world audience distribution.
- (factsheet) [factsheet:fans] Ticket QR encodes a per-ticket token of form '<ticket_id>.<hex_hmac>' (fulfillment migration), while the order page is reached via the order-level access_token — confirm the door-scanner/check-in UX if documenting it for fans.
- (factsheet) [factsheet:fans] Whether /public-calendar surfaces concerts/events to fans and how it relates to Box Office (not read in depth).
- (factsheet) [factsheet:addons] No gw_billing_modules seed row for module_id 'part_tracks' or 'studio' was found in supabase/migrations (viewer, concert_planner, box_office, store are seeded there). Since v_tenant_active_modules is built from gw_billing_modules, it's unclear how the nav gates hasModule('part_tracks')/hasModule('studio') ever resolve true. The rows may have been created directly on the self-hosted droplet (memory notes migrations run as postgres superuser) or seeded in a file not matched. Confirm against the live DB.
- (factsheet) [factsheet:addons] The CREATE statements for table gw_billing_modules and view v_tenant_active_modules were not located in the repo migrations (only INSERTs/comments reference them). Confirm their exact column set (especially whether the view unions all 'starter' rows for every tenant plus gw_tenant_subscriptions rows).
- (factsheet) [factsheet:addons] Two parallel Template-Course systems exist: CourseStorePage uses gw_courses + list_course_templates + adopt_course_template, while useCourseStore.ts uses gw_academy_courses + adopt_template_course (+ gw_course_product/gw_tenant_entitlement/create-course-checkout for paid courses). TemplateCoursePage reads gw_academy_courses. Confirm which is authoritative/live and whether paid course templates are actually offered (the wired store shows all as 'Free').
- (factsheet) [factsheet:addons] Box Office publish preconditions (public + >=1 tier with quantity, over-capacity block) are inferred from the file header comment and PublishCard wiring; the exact validation logic inside PublishCard was not read line-by-line.
- (factsheet) [factsheet:addons] The tenant-facing Billing tab reads gw_tenant_active_addons; that view/table's definition was not inspected. Confirm it reflects only paid (addon-tier) subscriptions and its monthly_price source.
- (factsheet) [factsheet:addons] Whether 'gw-demo-toggle-addon' is hard-limited to the demo tenant server-side (the client comment says it is 'hard-gated on server') was not verified in the edge function source.
- (factsheet) [factsheet:addons] Glee Academy is documented as an 'add-on' in the manual plan, but in code it is core/always-on. Confirm with product whether the manual should describe it as plan-dependent at all, or only its Course Store / per-course add-ons as optional.
- (getting-started/what-is-gleeworld.md) Does the marketing copy "set up in ten minutes" translate to an accurate claim about typical setup time we should quote to readers?
- (getting-started/accounts-and-roles.md) Should the graduate/auditioner/fan role home screens (/alumni, /auditioner, /fan) be presented to end users by URL, or only by their friendly names?
- (getting-started/the-mobile-app.md) Is there an Android version of the GleeWorld app, or is iOS the only mobile platform?
- (getting-started/the-mobile-app.md) Does Apple Music playback of backing tracks require the listener to have their own Apple Music subscription, and should we state that requirement to students?
- (getting-started/signing-in-and-getting-help.md) What are the password requirements enforced on the public sign-up form (the fact sheet does not specify them)?
- (getting-started/signing-in-and-getting-help.md) Is there a dedicated in-app support or help-desk channel beyond the /contact page and kevin@gleeworld.org?
- (tenants/program-setup.md) Does clicking Data export actually deliver an export bundle by email, given the gw-tenant-export edge function is noted as not yet wired?
- (tenants/content-management.md) What is the difference between the classic Media Library at /admin/media and the newer MediaLibraryPage at /dashboard/media-library, and which one should directors use?
- (tenants/content-management.md) What are the exact routes and capabilities of the Music Library and Music Toolkit pages for directors?
- (students/joining-and-first-sign-in.md) How do enrolled students receive their initial login credentials — an email invite, a temporary password, or a magic link?
- (students/part-tracks-and-practice.md) Is Part Tracks always included, or is it gated behind the Studio & Part Tracks add-on for the tenant?
- (students/part-tracks-and-practice.md) What determines which Part Tracks surface a given student sees — a tenant setting, or are both available at once?
- (students/studio-basics.md) Is Studio always included for students, or is it gated behind the Studio & Part Tracks add-on for the tenant?
- (fans/buying-tickets.md) Is there any self-service way for a buyer to request a refund, or must they always contact the organizer directly?
- (fans/attending-events.md) What does the public calendar at /public-calendar show to fans, and can they open ticket pages or programs directly from it?
- (add-ons/box-office.md) How buyers receive their ticket / QR code after purchase — the fact sheet does not state the delivery method.
- (add-ons/concert-planner.md) The public URL pattern for a published concert program — the fact sheet lists a `published_slug` field but not the full public path.
- (add-ons/studio-part-tracks.md) The billing tier / activation path for the `part_tracks` and `studio` modules — no `gw_billing_modules` seed row for either was found, so it is unclear whether they are starter, add-on, or activated some other way.
- (add-ons/template-courses.md) Two template systems appear to coexist in the code — the wired Course Store shows every template as "Free" and adopts via an RPC, while a parallel layer models purchasable course products (SKU, price, Stripe checkout) and per-tenant entitlements. Confirm which path is live for tenants and whether any template courses are actually paid.
- (faq/billing-and-payments.md) Does GleeWorld take 0% or 1% of paid ticket sales? Marketing and platform memory say 0% ("keep 100%"), but the box-office-checkout function applies a 1% application fee.
- (faq/privacy-and-data.md) Is there a canonical list of personal-data categories collected (beyond profile name/email/role) that should be documented here?
- (faq/privacy-and-data.md) Is the tenant data export (gw-tenant-export) live and actually delivering export bundles, or is it still only a confirmation message?
- (faq/ios-mobile-app.md) What is the minimum iOS version the GleeWorld app supports?
- (faq/ios-mobile-app.md) Is there an Android version of the GleeWorld app, or is it iOS-only?
